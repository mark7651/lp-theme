{
	"name": "lptheme",
	"version": "6.0.0",
	"description": "Hi. I'm a starter theme called lptheme",
	"author": "LPTeam",
	"keywords": [
		"WordPress",
		"Theme"
	],
	"license": "GPL-2.0-or-later",
	"scripts": {
		"watch": "npx tailwindcss -i ./tailwind.css -o ./style.css --watch",
		"browser-sync": "browser-sync start --proxy 'nowhere.local' --files '**/*.php, **/*.css, **/**/*.css, **/*.js'"
	},
	"devDependencies": {
		"browser-sync": "^3.0.3",
		"tailwind-scrollbar": "^3.1.0",
		"tailwindcss": "^3.4.15"
	}
}
