{
	"name": "lptheme",
	"version": "1.0.0",
	"description": "lptheme",
	"scripts": {
		"watch": "npx @tailwindcss/cli -i ./tailwind.css -o ./style.css --watch",
		"browser-sync": "browser-sync start --proxy 'lptheme.local' --files '**/*.php, **/*.css, **/**/*.css, **/*.js'"
	},
	"devDependencies": {
		"browser-sync": "^3.0.3",
		"tailwindcss": "^4.0.4"
	},
	"dependencies": {
		"@tailwindcss/cli": "^4.0.4"
	}
}
